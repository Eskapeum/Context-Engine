# UCE Inheritance

Show class inheritance hierarchy.

## Instructions

Run the following command with the class name:

```bash
npx uce inheritance "$ARGUMENTS"
```

This displays:
- Parent classes (extends/implements)
- Child classes (extended by)
- Full inheritance tree

Example:
```bash
npx uce inheritance BaseController
npx uce inheritance UserService
```
